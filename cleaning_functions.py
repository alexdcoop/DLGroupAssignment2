#CREATE FUNCTIONS FOR USE WITH GENERAL DOC CLEANING

def clean_data(dataset):
    nulls_present = dataset.isnull().sum().sum() #check for null values
    if nulls_present > 0:

    return clean_df

    pricing.isnull().sum().sum()